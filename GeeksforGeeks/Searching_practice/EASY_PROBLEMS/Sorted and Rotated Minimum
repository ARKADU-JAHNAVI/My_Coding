#User function Template for python3

class Solution:
    def findMin(self, arr):
        n = len(arr)
        res = arr[0]
        for i in range(1,n):
            res = min(res,arr[i])
        return res
        